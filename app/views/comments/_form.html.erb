

<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <p>
    <%= form.label :Name %><br>
    <%= form.text_field :commenter %>
  </p>
  <p>
    <%= form.label :Comment %><br>
    <%= form.text_area :body, class: "comment_entry" %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
